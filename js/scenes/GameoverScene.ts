import { Scenes } from "../const";

export default class GameoverScene extends Phaser.Scene {
  constructor() {
    super({
      key: Scenes.GAME_OVER,
    });
  }

  init() {}
  preload() {}
  create() {}
}
